# Configuration

Bot can be configured via process env.

```
"ENV": - default: "development"
"TOKEN" - required
```

#Local development

```$xslt
npm install
npm run serve-dev
```

#Production Setup

```$xslt
npm install
npm run build
npm run serve-prod
```
